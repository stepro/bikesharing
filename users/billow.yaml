name: bikesharing/users
type: helm
dependencies:
  db: bikesharing/users-db
chart: charts/users
set:
  image:
    tag: $(build.image):$(build.tag)
  secrets:
    data: $(db.connectionString)
exports:
  endpoint: $(self.values.ingress.hosts[0])

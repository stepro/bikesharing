name: bikesharing/users
dependencies:
  mongo: ./db
components:
  api:
    type: helm
    chart: charts/users
    set:
      image:
        tag: $(build.image):$(build.tag)
      data: $(mongo.connectionString)
exports:
  endpoint: $(api.values.ingress.hosts[0])

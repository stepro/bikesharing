name: bikesharing/billing
dependencies:
  data:
    type: mongo
    roles: [readWrite]
environment:
  MONGO_CONNECTION_STRING: $(data.connectionString)
development:
  refresh:
    killProcesses: [app]
    rebuildCommands:
    - go install -v
name: bikesharing/billing
dependencies:
  sql: ./db
components:
  functions:
    type: function-group
    environment:
      runtime: dotnet:2.1-runtime-alpine
      variables:
        SQL_CONNECTION_STRING: $(sql.connectionString)
exports:
  endpoint: $(functions.endpoint)

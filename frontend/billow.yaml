name: bikesharing/frontend
dependencies:
  api: bikesharing/api
configuration:
- static/settings.js
release:
  type: static-website
  build:
    commands:
    - npm install
    - npm run build
    - npm run export
    result: out

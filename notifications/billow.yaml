name: bikesharing/notifications
components:
  queue:
    type: azure/service-bus-queue
  emailAdapter:
    type: container
    directory: adapters/email
    environment:
      variables:
        $(queue.namespace.connectionString)
  twilioAdapter:
    type: function-group
    directory: adapters/twilio
    environment:
      variables:
        $(queue.namespace.connectionString)
exports:
  connectionString: $(queue.connectionString)
